@model IEnumerable<TrainTickets.Models.Station>

@section Styles {
    <link rel="stylesheet" href="~/css/animations.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/index.css" asp-append-version="true" />
}

<div class="page-container fade-in">
    <div class="page-header">
        <h1 class="page-title">ЖП Гари</h1>

        <p>
            <a asp-action="Create" class="btn-primary-custom">+ Създай нова гара</a>
        </p>
    </div>
    <div class="custom-table-wrapper">
        <table class="custom-table">
            <thead>
                <tr>
                    <th>Име на гарата</th>
                    <th>Град</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.City)
                        </td>
                        <td class="actions">
                            <a asp-action="Edit" asp-route-id="@item.Id" class="action-btn edit">Редактиране</a>
                            <a asp-action="Details" asp-route-id="@item.Id" class="action-btn details">Детайли</a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="action-btn delete">Изтриване</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>